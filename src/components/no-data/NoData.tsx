import styles from "./no-data.module.scss";

const NoData = () => {
    return (
        <div className={styles.no_data}>
            <h2>No Data Found</h2>
        </div>
    )
}

export default NoData;